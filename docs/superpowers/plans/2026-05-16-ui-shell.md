# UI シェル Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `apps/web` と `apps/admin` に shadcn/ui (Base UI + Mira スタイル) のシェル（ヘッダー + ダークモード + admin のサイドバー）を導入し、ローカル dev で両 app の見た目が立ち上がる状態にする。

**Architecture:**

- shadcn 4.7 を `--base base --preset mira` で各 app に init（Base UI primitives、Mira スタイル）
- 共通コンポーネント: `AppHeader` + `ThemeProvider` + `ModeToggle`
- admin のみ: `AppSidebar`（shadcn の Sidebar primitive 利用）
- ダークモードは TanStack Start の `ScriptOnce` を使った shadcn 公式パターン（外部 lib なし）

**Tech Stack:** shadcn/ui 4.7 + `@base-ui-components/react` + TanStack Start + Tailwind CSS v4 + lucide-react

**スコープ外（別プラン）:**

- 認証 / Better Auth クライアントの組み込み
- API クライアント（orval）の生成
- 3-column タイムラインレイアウト等の実機能ページ
- `packages/` への UI 共通化

---

## File Structure

```
apps/web/
├── components.json                 # NEW (shadcn init)
├── src/
│   ├── components/
│   │   ├── ui/                     # NEW (shadcn add)
│   │   │   ├── button.tsx
│   │   │   ├── avatar.tsx
│   │   │   └── dropdown-menu.tsx
│   │   ├── app-header.tsx          # NEW
│   │   ├── theme-provider.tsx      # NEW
│   │   └── mode-toggle.tsx         # NEW
│   ├── lib/
│   │   └── utils.ts                # NEW (shadcn cn helper)
│   ├── routes/
│   │   ├── __root.tsx              # MODIFY
│   │   └── index.tsx               # MODIFY
│   └── styles.css                  # MODIFY (shadcn の Mira CSS が追加される)

apps/admin/  (同上 + 追加で)
├── src/
│   ├── components/
│   │   ├── ui/
│   │   │   ├── sidebar.tsx         # NEW (shadcn add)
│   │   │   ├── sheet.tsx           # NEW (sidebar の依存)
│   │   │   ├── separator.tsx       # NEW (同上)
│   │   │   └── tooltip.tsx         # NEW (同上)
│   │   └── app-sidebar.tsx         # NEW
```

**Design decisions:**

- `theme-provider.tsx` と `mode-toggle.tsx` は **両 app に完全同一のコピーを直置き**（spec の方針）
- ThemeProvider / ModeToggle は shadcn 公式 TanStack Start ガイドの実装をそのまま採用
- `__root.tsx` は既存の `shellComponent: RootDocument` パターンを保持し、内部だけ差し替え
- 既存の `TanStackDevtools` 設定はそのまま残す
- shadcn のセットアップ後に `iconLibrary` を `lucide` に修正する（Mira プリセットのデフォルトは hugeicons）

---

## Task 1: apps/web に shadcn を init

**Files:**

- Create: `apps/web/components.json`
- Create: `apps/web/src/lib/utils.ts`
- Modify: `apps/web/src/styles.css`
- Modify: `apps/web/package.json`（shadcn が deps を追加）
- Modify: `pnpm-lock.yaml`

- [ ] **Step 1: apps/web ディレクトリに移動して shadcn init**

```bash
cd /Users/hwld/work/projects/evodo-sns/apps/web
pnpm dlx shadcn@latest init --base base --preset mira -y
```

Expected:

- `components.json` が生成される
- `src/lib/utils.ts` に `cn` ヘルパー追加
- `src/styles.css` に shadcn の Mira スタイル CSS + light/dark テーマ変数が追加される
- `@base-ui-components/react`、`clsx`、`tailwind-merge`、`class-variance-authority` などが `package.json` に追加される

CLI が対話的な確認を求めてきたら、`-y` でスキップされない部分について素直に Yes（既存の `styles.css` 上書き等は OK）。

- [ ] **Step 2: components.json の iconLibrary を `lucide` に修正**

Mira プリセットは `hugeicons` をデフォルトにするが、我々は `lucide-react` を catalog 経由で既に使う方針なので変更する。

`apps/web/components.json` の `iconLibrary` フィールドを編集:

```jsonc
{
  // ...
  "iconLibrary": "lucide",
  // ...
}
```

`hugeicons` がもし `package.json` に追加されていたら、`apps/web/package.json` の `dependencies` から削除して `pnpm install` し直す（任意、未使用なら残しても害は薄い）。

- [ ] **Step 3: typecheck + lint を確認**

Run（リポジトリルートで）:

```bash
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 4: commit**

```bash
cd /Users/hwld/work/projects/evodo-sns
git add apps/web/components.json apps/web/src/lib/utils.ts apps/web/src/styles.css apps/web/package.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(web): shadcn/ui を Base UI + Mira スタイルで init

apps/web に shadcn 4.7 を導入。Base UI primitives、Mira スタイル、
neutral カラーパレットの構成。アイコンライブラリは catalog 既定の
lucide-react に合わせて hugeicons から lucide に上書き。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: apps/admin に shadcn を init

**Files:**

- Create: `apps/admin/components.json`
- Create: `apps/admin/src/lib/utils.ts`
- Modify: `apps/admin/src/styles.css`
- Modify: `apps/admin/package.json`
- Modify: `pnpm-lock.yaml`

- [ ] **Step 1: apps/admin に shadcn init**

```bash
cd /Users/hwld/work/projects/evodo-sns/apps/admin
pnpm dlx shadcn@latest init --base base --preset mira -y
```

Expected: Task 1 と同等の変更が apps/admin 配下に発生。

- [ ] **Step 2: components.json の iconLibrary を `lucide` に修正**

`apps/admin/components.json` の `iconLibrary` を `lucide` に編集。

- [ ] **Step 3: typecheck + lint を確認**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 4: commit**

```bash
git add apps/admin/components.json apps/admin/src/lib/utils.ts apps/admin/src/styles.css apps/admin/package.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(admin): shadcn/ui を Base UI + Mira スタイルで init

apps/admin に shadcn 4.7 を導入。設定は apps/web と完全に同一
（Base UI primitives、Mira スタイル、neutral、lucide）。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 両 app に基本コンポーネント (button / avatar / dropdown-menu) を追加

**Files:**

- Create: `apps/web/src/components/ui/button.tsx`
- Create: `apps/web/src/components/ui/avatar.tsx`
- Create: `apps/web/src/components/ui/dropdown-menu.tsx`
- Create: `apps/admin/src/components/ui/button.tsx`
- Create: `apps/admin/src/components/ui/avatar.tsx`
- Create: `apps/admin/src/components/ui/dropdown-menu.tsx`

- [ ] **Step 1: apps/web に追加**

```bash
cd /Users/hwld/work/projects/evodo-sns/apps/web
pnpm dlx shadcn@latest add button avatar dropdown-menu
```

Expected: `src/components/ui/{button,avatar,dropdown-menu}.tsx` が生成される。lucide-react と base-ui-components の import が含まれる。

- [ ] **Step 2: apps/admin に追加**

```bash
cd /Users/hwld/work/projects/evodo-sns/apps/admin
pnpm dlx shadcn@latest add button avatar dropdown-menu
```

- [ ] **Step 3: typecheck + lint を確認**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 4: commit**

```bash
git add apps/web/src/components/ui apps/admin/src/components/ui
git commit -m "$(cat <<'EOF'
feat(ui): button / avatar / dropdown-menu を両 app に追加

shadcn add で apps/web と apps/admin に共通の 3 コンポーネントを生成。
両 app に同じ内容のファイルを直置きする方針（packages/ への切り出しは
重複が痛くなってから検討）。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: apps/admin に sidebar コンポーネント群を追加

**Files:**

- Create: `apps/admin/src/components/ui/sidebar.tsx`
- Create: `apps/admin/src/components/ui/sheet.tsx`
- Create: `apps/admin/src/components/ui/separator.tsx`
- Create: `apps/admin/src/components/ui/tooltip.tsx`
- 場合により `apps/admin/src/components/ui/input.tsx`、`label.tsx`、`skeleton.tsx` 等の依存も追加される

- [ ] **Step 1: shadcn add sidebar**

```bash
cd /Users/hwld/work/projects/evodo-sns/apps/admin
pnpm dlx shadcn@latest add sidebar
```

Expected: `sidebar.tsx` と依存（sheet / separator / tooltip 等）が `src/components/ui/` に生成される。`pnpm-lock.yaml` も更新される可能性あり（`vaul` などの依存）。

- [ ] **Step 2: typecheck + lint を確認**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 3: commit**

```bash
git add apps/admin/src/components/ui apps/admin/package.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
feat(admin): sidebar コンポーネント（依存込み）を追加

shadcn の Sidebar プリミティブとその依存（sheet / separator / tooltip 等）を
apps/admin にのみ追加。apps/web には不要。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: ThemeProvider と ModeToggle を両 app に作成

**Files:**

- Create: `apps/web/src/components/theme-provider.tsx`
- Create: `apps/web/src/components/mode-toggle.tsx`
- Create: `apps/admin/src/components/theme-provider.tsx`
- Create: `apps/admin/src/components/mode-toggle.tsx`

両 app の `theme-provider.tsx` と `mode-toggle.tsx` は完全に同一の内容。

- [ ] **Step 1: theme-provider.tsx の内容を準備**

両 app の `src/components/theme-provider.tsx` に以下を保存:

```tsx
import { createContext, useContext, useEffect, useState } from "react";
import { ScriptOnce } from "@tanstack/react-router";

type Theme = "dark" | "light" | "system";

type ThemeProviderProps = {
  children: React.ReactNode;
  defaultTheme?: Theme;
  storageKey?: string;
};

type ThemeProviderState = {
  theme: Theme;
  setTheme: (theme: Theme) => void;
};

function getThemeScript(storageKey: string, defaultTheme: Theme) {
  const key = JSON.stringify(storageKey);
  const fallback = JSON.stringify(defaultTheme);

  return `(function(){try{var t=localStorage.getItem(${key});if(t!=='light'&&t!=='dark'&&t!=='system'){t=${fallback}}var d=matchMedia('(prefers-color-scheme: dark)').matches;var r=t==='system'?(d?'dark':'light'):t;var e=document.documentElement;e.classList.add(r);e.style.colorScheme=r}catch(e){}})();`;
}

const ThemeProviderContext = createContext<ThemeProviderState>({
  theme: "system",
  setTheme: () => {},
});

function applyTheme(theme: Theme) {
  const root = document.documentElement;
  root.classList.remove("light", "dark");

  const resolved =
    theme === "system"
      ? window.matchMedia("(prefers-color-scheme: dark)").matches
        ? "dark"
        : "light"
      : theme;

  root.classList.add(resolved);
  root.style.colorScheme = resolved;
}

export function ThemeProvider({
  children,
  defaultTheme = "system",
  storageKey = "theme",
}: ThemeProviderProps) {
  const [theme, setThemeState] = useState<Theme>(defaultTheme);
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    const stored = localStorage.getItem(storageKey);
    setThemeState(
      stored === "light" || stored === "dark" || stored === "system"
        ? stored
        : defaultTheme,
    );
    setMounted(true);
  }, [defaultTheme, storageKey]);

  useEffect(() => {
    if (!mounted) return;
    applyTheme(theme);
  }, [theme, mounted]);

  useEffect(() => {
    if (!mounted || theme !== "system") return;

    const media = window.matchMedia("(prefers-color-scheme: dark)");
    const onChange = () => applyTheme("system");
    media.addEventListener("change", onChange);
    return () => media.removeEventListener("change", onChange);
  }, [theme, mounted]);

  const setTheme = (next: Theme) => {
    localStorage.setItem(storageKey, next);
    setThemeState(next);
  };

  return (
    <ThemeProviderContext value={{ theme, setTheme }}>
      <ScriptOnce>{getThemeScript(storageKey, defaultTheme)}</ScriptOnce>
      {children}
    </ThemeProviderContext>
  );
}

export function useTheme() {
  const context = useContext(ThemeProviderContext);
  if (context === undefined)
    throw new Error("useTheme must be used within a ThemeProvider");
  return context;
}
```

- [ ] **Step 2: mode-toggle.tsx の内容を準備**

両 app の `src/components/mode-toggle.tsx` に以下を保存:

```tsx
import { Moon, Sun } from "lucide-react";

import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { useTheme } from "@/components/theme-provider";

export function ModeToggle() {
  const { setTheme } = useTheme();

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="outline" size="icon">
          <Sun className="h-[1.2rem] w-[1.2rem] scale-100 rotate-0 transition-all dark:scale-0 dark:-rotate-90" />
          <Moon className="absolute h-[1.2rem] w-[1.2rem] scale-0 rotate-90 transition-all dark:scale-100 dark:rotate-0" />
          <span className="sr-only">Toggle theme</span>
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem onClick={() => setTheme("light")}>
          Light
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("dark")}>
          Dark
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("system")}>
          System
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

- [ ] **Step 3: typecheck + lint を確認**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 4: commit**

```bash
git add apps/web/src/components/theme-provider.tsx apps/web/src/components/mode-toggle.tsx apps/admin/src/components/theme-provider.tsx apps/admin/src/components/mode-toggle.tsx
git commit -m "$(cat <<'EOF'
feat(ui): ThemeProvider と ModeToggle を両 app に追加

shadcn の TanStack Start 公式ダークモードガイド準拠の実装。
ScriptOnce で hydration 前にテーマを適用、light/dark/system の 3 値、
localStorage 永続化。両 app に完全同一のコピーを直置き。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: apps/web の AppHeader と \_\_root.tsx / index.tsx を実装

**Files:**

- Create: `apps/web/src/components/app-header.tsx`
- Modify: `apps/web/src/routes/__root.tsx`
- Modify: `apps/web/src/routes/index.tsx`

- [ ] **Step 1: AppHeader を作成**

`apps/web/src/components/app-header.tsx`:

```tsx
import { Link } from "@tanstack/react-router";

import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { ModeToggle } from "@/components/mode-toggle";

export function AppHeader() {
  return (
    <header className="border-b">
      <div className="container mx-auto flex h-14 items-center justify-between px-4">
        <Link to="/" className="font-semibold tracking-tight">
          evodo
        </Link>
        <div className="flex items-center gap-2">
          <ModeToggle />
          <DropdownMenu>
            <DropdownMenuTrigger asChild>
              <Button variant="ghost" size="icon" className="rounded-full">
                <Avatar className="h-8 w-8">
                  <AvatarFallback>?</AvatarFallback>
                </Avatar>
              </Button>
            </DropdownMenuTrigger>
            <DropdownMenuContent align="end">
              <DropdownMenuItem>Sign in</DropdownMenuItem>
            </DropdownMenuContent>
          </DropdownMenu>
        </div>
      </div>
    </header>
  );
}
```

- [ ] **Step 2: \_\_root.tsx を更新**

`apps/web/src/routes/__root.tsx` を以下に置き換え:

```tsx
import { HeadContent, Scripts, createRootRoute } from "@tanstack/react-router";
import { TanStackRouterDevtoolsPanel } from "@tanstack/react-router-devtools";
import { TanStackDevtools } from "@tanstack/react-devtools";

import { AppHeader } from "@/components/app-header";
import { ThemeProvider } from "@/components/theme-provider";
import appCss from "../styles.css?url";

export const Route = createRootRoute({
  head: () => ({
    meta: [
      {
        charSet: "utf-8",
      },
      {
        name: "viewport",
        content: "width=device-width, initial-scale=1",
      },
      {
        title: "evodo",
      },
    ],
    links: [
      {
        rel: "stylesheet",
        href: appCss,
      },
    ],
  }),
  shellComponent: RootDocument,
});

function RootDocument({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja" suppressHydrationWarning>
      <head>
        <HeadContent />
      </head>
      <body>
        <ThemeProvider defaultTheme="system" storageKey="theme">
          <AppHeader />
          <main className="container mx-auto p-4">{children}</main>
        </ThemeProvider>
        <TanStackDevtools
          config={{
            position: "bottom-right",
          }}
          plugins={[
            {
              name: "Tanstack Router",
              render: <TanStackRouterDevtoolsPanel />,
            },
          ]}
        />
        <Scripts />
      </body>
    </html>
  );
}
```

- [ ] **Step 3: index.tsx を更新**

`apps/web/src/routes/index.tsx` を以下に置き換え:

```tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/")({
  component: Home,
});

function Home() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">evodo</h1>
      <p className="text-muted-foreground">
        1 投稿 = 1 TODO リスト の SNS（実装中）
      </p>
    </div>
  );
}
```

- [ ] **Step 4: typecheck + lint を確認**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 5: commit**

```bash
git add apps/web/src/components/app-header.tsx apps/web/src/routes/__root.tsx apps/web/src/routes/index.tsx
git commit -m "$(cat <<'EOF'
feat(web): AppHeader を導入し __root.tsx と index.tsx を更新

ThemeProvider でラップした上で、ヘッダー（ブランディング + ModeToggle +
ユーザーメニュー placeholder）と main 領域を構築。index.tsx は
シェル確認用の placeholder コンテンツに差し替え。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: apps/admin の AppSidebar / AppHeader と \_\_root.tsx / index.tsx を実装

**Files:**

- Create: `apps/admin/src/components/app-sidebar.tsx`
- Create: `apps/admin/src/components/app-header.tsx`
- Modify: `apps/admin/src/routes/__root.tsx`
- Modify: `apps/admin/src/routes/index.tsx`

- [ ] **Step 1: AppSidebar を作成**

`apps/admin/src/components/app-sidebar.tsx`:

```tsx
import { Link } from "@tanstack/react-router";
import { FileText, Home, Users } from "lucide-react";

import {
  Sidebar,
  SidebarContent,
  SidebarGroup,
  SidebarGroupContent,
  SidebarGroupLabel,
  SidebarHeader,
  SidebarMenu,
  SidebarMenuButton,
  SidebarMenuItem,
} from "@/components/ui/sidebar";

const items = [
  { title: "Dashboard", url: "#", icon: Home },
  { title: "Users", url: "#", icon: Users },
  { title: "Posts", url: "#", icon: FileText },
];

export function AppSidebar() {
  return (
    <Sidebar>
      <SidebarHeader>
        <SidebarMenu>
          <SidebarMenuItem>
            <SidebarMenuButton size="lg" asChild>
              <Link to="/">
                <span className="font-semibold tracking-tight">
                  evodo admin
                </span>
              </Link>
            </SidebarMenuButton>
          </SidebarMenuItem>
        </SidebarMenu>
      </SidebarHeader>
      <SidebarContent>
        <SidebarGroup>
          <SidebarGroupLabel>運用</SidebarGroupLabel>
          <SidebarGroupContent>
            <SidebarMenu>
              {items.map((item) => (
                <SidebarMenuItem key={item.title}>
                  <SidebarMenuButton asChild>
                    <a href={item.url}>
                      <item.icon />
                      <span>{item.title}</span>
                    </a>
                  </SidebarMenuButton>
                </SidebarMenuItem>
              ))}
            </SidebarMenu>
          </SidebarGroupContent>
        </SidebarGroup>
      </SidebarContent>
    </Sidebar>
  );
}
```

- [ ] **Step 2: AppHeader を作成**

`apps/admin/src/components/app-header.tsx`:

```tsx
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { SidebarTrigger } from "@/components/ui/sidebar";
import { ModeToggle } from "@/components/mode-toggle";

export function AppHeader() {
  return (
    <header className="border-b">
      <div className="flex h-14 items-center justify-between gap-2 px-4">
        <div className="flex items-center gap-2">
          <SidebarTrigger />
          <span className="font-semibold tracking-tight">evodo admin</span>
        </div>
        <div className="flex items-center gap-2">
          <ModeToggle />
          <DropdownMenu>
            <DropdownMenuTrigger asChild>
              <Button variant="ghost" size="icon" className="rounded-full">
                <Avatar className="h-8 w-8">
                  <AvatarFallback>?</AvatarFallback>
                </Avatar>
              </Button>
            </DropdownMenuTrigger>
            <DropdownMenuContent align="end">
              <DropdownMenuItem>Sign in</DropdownMenuItem>
            </DropdownMenuContent>
          </DropdownMenu>
        </div>
      </div>
    </header>
  );
}
```

- [ ] **Step 3: \_\_root.tsx を更新**

`apps/admin/src/routes/__root.tsx` を以下に置き換え:

```tsx
import { HeadContent, Scripts, createRootRoute } from "@tanstack/react-router";
import { TanStackRouterDevtoolsPanel } from "@tanstack/react-router-devtools";
import { TanStackDevtools } from "@tanstack/react-devtools";

import { AppHeader } from "@/components/app-header";
import { AppSidebar } from "@/components/app-sidebar";
import { ThemeProvider } from "@/components/theme-provider";
import { SidebarInset, SidebarProvider } from "@/components/ui/sidebar";
import appCss from "../styles.css?url";

export const Route = createRootRoute({
  head: () => ({
    meta: [
      {
        charSet: "utf-8",
      },
      {
        name: "viewport",
        content: "width=device-width, initial-scale=1",
      },
      {
        title: "evodo admin",
      },
    ],
    links: [
      {
        rel: "stylesheet",
        href: appCss,
      },
    ],
  }),
  shellComponent: RootDocument,
});

function RootDocument({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ja" suppressHydrationWarning>
      <head>
        <HeadContent />
      </head>
      <body>
        <ThemeProvider defaultTheme="system" storageKey="theme">
          <SidebarProvider>
            <AppSidebar />
            <SidebarInset>
              <AppHeader />
              <main className="p-4">{children}</main>
            </SidebarInset>
          </SidebarProvider>
        </ThemeProvider>
        <TanStackDevtools
          config={{
            position: "bottom-right",
          }}
          plugins={[
            {
              name: "Tanstack Router",
              render: <TanStackRouterDevtoolsPanel />,
            },
          ]}
        />
        <Scripts />
      </body>
    </html>
  );
}
```

- [ ] **Step 4: index.tsx を更新**

`apps/admin/src/routes/index.tsx` を以下に置き換え:

```tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/")({
  component: AdminHome,
});

function AdminHome() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">evodo admin</h1>
      <p className="text-muted-foreground">管理画面のシェル（実装中）</p>
    </div>
  );
}
```

- [ ] **Step 5: typecheck + lint を確認**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm typecheck && pnpm lint
```

Expected: 通る

- [ ] **Step 6: commit**

```bash
git add apps/admin/src/components/app-sidebar.tsx apps/admin/src/components/app-header.tsx apps/admin/src/routes/__root.tsx apps/admin/src/routes/index.tsx
git commit -m "$(cat <<'EOF'
feat(admin): AppSidebar / AppHeader と __root.tsx / index.tsx を更新

shadcn の Sidebar プリミティブを使った左サイドバー（Dashboard / Users /
Posts の placeholder メニュー）と、SidebarTrigger 付きの AppHeader を
配置。ThemeProvider と SidebarProvider で全体をラップ。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 手動 動作確認

**Files:** なし（コード書かない）

`pnpm run dev` で両 app を起動して、以下を目視確認する。

- [ ] **Step 1: dev サーバ起動**

```bash
cd /Users/hwld/work/projects/evodo-sns
pnpm run dev
```

`tmp/dev.log` にログが流れる。`apps/web` は `http://localhost:3000`、`apps/admin` は `http://localhost:3001`、`apps/api` は `http://localhost:8787`。

- [ ] **Step 2: apps/web を確認**

ブラウザで `http://localhost:3000` を開く。期待:

- ヘッダーに「evodo」（左）と ModeToggle + Avatar アイコン（右）
- main 領域に「evodo」見出しと placeholder 説明文
- 初期テーマは OS 設定に追従（システムが dark なら dark）
- ModeToggle をクリック → Light / Dark / System が選択でき、即座に画面の色が切り替わる
- ページリロード時に flash（白光り）が起きないこと

- [ ] **Step 3: apps/admin を確認**

ブラウザで `http://localhost:3001` を開く。期待:

- 左サイドバーに「evodo admin」ヘッダーと Dashboard / Users / Posts のメニュー（アイコン付き）
- 上部ヘッダーに SidebarTrigger（左端）と ModeToggle + Avatar（右端）
- main 領域に「evodo admin」見出しと placeholder
- モバイル幅（DevTools で画面サイズを縮める）にすると、サイドバーが消えて SidebarTrigger 押下でドロワーとして開く
- ダークモード切り替えが効くこと

- [ ] **Step 4: 後片付け**

dev サーバを停止（`Ctrl+C` または起動した shell を閉じる）。

---

## Self-Review メモ（プラン作成者向け、実装者は無視可）

**Spec coverage:**

- §2 shadcn セットアップ → Task 1, 2（init）、Task 3（基本コンポーネント）、Task 4（admin の sidebar）
- §3 レイアウト構造 → Task 6（web）、Task 7（admin）
- §4 ファイル構成 → Task 1〜7 で全ファイル網羅
- §5 コンポーネント仕様 → AppHeader（Task 6, 7）、AppSidebar（Task 7）、ThemeProvider（Task 5）、ModeToggle（Task 5）、\_\_root.tsx（Task 6, 7）、index.tsx（Task 6, 7）
- §6 ダークモード → Task 5 + ScriptOnce 統合
- §7 動作確認 → Task 8

**Placeholder scan:** なし

**Type consistency:**

- `AppHeader` / `AppSidebar` / `ThemeProvider` / `ModeToggle` の名前が全タスクで一貫
- shadcn 生成物（`Button` / `Avatar` / `DropdownMenu` / `Sidebar` / `SidebarTrigger` / `SidebarInset` / `SidebarProvider`）の import 元はすべて `@/components/ui/*`
- `useTheme` の戻り値型は theme-provider.tsx で定義済み、ModeToggle で消費

**潜在的なつまずきポイント:**

- shadcn init が既存の `styles.css` の独自 `box-sizing` などを上書きする可能性 → 必要なら implementer が手動でマージ
- `--preset mira` で `hugeicons` がインストールされたら、Step 2 で `iconLibrary` を `lucide` に書き換える対応が必要
- shadcn add で生成される component が `noUnusedLocals` / `noUnusedParameters` 違反する可能性は低いが、稀に発生する場合は調整
