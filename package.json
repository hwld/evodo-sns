{
  "name": "evodo-sns",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "mkdir -p tmp && turbo run dev 2>&1 | tee tmp/dev.log",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check-types": "turbo run check-types",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "catalog:"
  },
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=22"
  }
}
